{"verdict":"REVISE","findings":[{"id":"F-01","severity":"high","section":"Implementation slices and acceptance","claim":"AI-1 is presented as the first slice to build and measure, yet its own row requires full placement authority (E-012 ledger, E-013 placement), which the repo audit shows is absent (voice resolver returns locations: []) and which scope.csv defers from S.","why":"The stated sequence cannot start; 'measure work on AI-1 before estimating later slices' silently makes the ledger/placement outcomes the critical path without saying so, and invites relabeling the legacy inventory_items/bins projection as the concierge answer.","evidence":"src/app/api/cellar/voice-resolve/handler.ts:252; src/app/api/search/route.ts:155; docs/plans/2026-09-04-terroir-rebuild/scope.csv:45 (E-044 no S implementation); canonical boundary: 'No legacy unified search fallback is permitted'.","required_change":"State that no AI slice enters S. Make AI-1 explicitly gated on the target StockCommand/Receipt balance projection, and allow AI-2 to be the first admitted slice if E-001/E-036/E-038 land before E-012/E-013. Replace 'measure AI-1 first' with 'measure the first admitted slice'."},{"id":"F-02","severity":"medium","section":"What the two examples require / AI-3","claim":"'Nine eligible, deduplicated wine experiences' has no frozen grain: canonical wine vs variant/vintage, which event types count, ordering key, ties, time window, and whether same-wine repeat tastings are one or many experiences.","why":"AI-3's exit criterion says counts must be exact, but exactness is untestable without the definition; two reviewers could both pass and disagree.","evidence":"Draft: 'deduplicates experiences and wine identities, separates event types'; identity is two-grain in repo (canonical_wines producer+cuvée, wine_variants +vintage+size).","required_change":"Freeze before AI-3 admission: experience grain = one tasting/acquisition event per (profile, wine_variant or canonical fallback, date); count is per experience not per bottle; ordering by tasted_at then recorded_at; disclose grain in the response."},{"id":"F-03","severity":"medium","section":"Implementation slices and acceptance","claim":"Which slices have a hard dependency on a production LLM provider is not stated, while the provider is unselected and uncosted.","why":"AI-1 name resolution and clarification, and AI-2 record management, can be fully deterministic (existing fuzzy search + candidate list). If the draft binds them to conversational inference, an unresolved provider decision blocks the two safest slices.","evidence":"Draft: 'deterministic lookup and recommendation cards must continue to work when the conversational provider is unavailable'; src/app/api/assistant/route.ts:1 is model-free.","required_change":"Classify per slice: AI-1, AI-2, AI-3 core = LLM-optional (phrasing layer only, deterministic fallback is the acceptance path); AI-4 answers and AI-5 free-form concierge = LLM-required. Provider selection gates only the latter."},{"id":"F-04","severity":"medium","section":"Where memory, ratings and recommendations live","claim":"The taste-profile entity already carries household sharing, cross-context grants, permitted signal classes and gift-hypothesis rules; the opening frames the design for 'the whole wine community'.","why":"These are F-scale permission features, not requirements of the two examples; encoding them in AI-2's data contract is hidden scope growth that D must design around before any user outcome ships.","evidence":"Entity table row 1; draft intro paragraph; canonical boundary limits candidate lookup to approved catalog projection.","required_change":"Trim AI-2 to single-actor personal profile plus business-owned notes with no cross-context grants. Record household/grant/gift semantics as named unresolved F items, not entity fields."},{"id":"F-05","severity":"medium","section":"Evaluation and release rules / slice exit evidence","claim":"Safety tests are unevenly assigned: forged/revoked scope and cost leakage appear only under AI-1; prompt injection only under AI-4; LLM-supplied profile-ID rejection appears in prose but in no slice's exit evidence.","why":"AI-3 price suggestions can leak unit cost to actors without cost grants; wine names and user notes injected into tool results reach the model in AI-1/AI-3 too; profile spoofing is an AI-2/AI-3 attack, not an AI-1 one.","evidence":"Slice table rows AI-2, AI-3, AI-5; entity table 'an LLM-supplied profile ID does not grant access'; canonical: 'Costs and supplier terms need separate grants'.","required_change":"Make a common safety matrix (scope forgery, revoked grant mid-request, cost/supplier leakage, injection via record text, profile-ID spoofing, deletion race) a blocking exit criterion for every slice, not per-slice extras."},{"id":"F-06","severity":"medium","section":"AI-4 Knowledge and optional semantic retrieval","claim":"AI-4 is said to proceed independently once rights prerequisites exist, but no licensed prose corpus is identified; the repo holds X-Wines aggregates (ratings, not knowledge text), and WineSensed/X-Wines are excluded from personalization/training.","why":"The 'independent' path is blocked on an unmade corpus/rights decision; the embedding shortlist has nothing to embed.","evidence":"Repo audit: X-Wines rows at src/types/database.ts:2300/2408 are aggregate ratings; draft: E-024/E-026 rights deferred.","required_change":"Mark AI-4 blocked on a named corpus decision (house notes with author consent, licensed reference text, or none). Do not estimate it until then."},{"id":"F-07","severity":"low","section":"Recommendation process / GitHub table","claim":"RecTools/LightFM/implicit are Python; Terroir is TypeScript on Railway. 'Narrow dependencies' understates that AI-6 introduces a second runtime and an undecided serving path.","why":"Offline harness is fine, but how learned scores reach the web app (batch-written to Postgres vs a Python service) determines whether Gorse-like operating cost returns through the back door.","evidence":"rec-005 install extras; canonical: 'Do not add ... microservices without measured need'; LightFM pushed_at 2024-07-24.","required_change":"State AI-6's serving contract now as a decision to make at admission: default = batch scores/features written into Postgres by an offline job, no online Python service."},{"id":"F-08","severity":"low","section":"Technology licensing","claim":"pgvector's license is not stated; GitHub metadata reports NOASSERTION for both pgvector and vercel/ai.","why":"The draft correctly reads the vercel/ai LICENSE file text; the same file-level check is missing for pgvector, and Qwen/BGE 'model repository lists' claims rest on card text, not a rights audit (draft admits).","evidence":"ROOT LICENSE/API METADATA: pgvector spdx NOASSERTION; vercel/ai spdx NOASSERTION with LICENSE text Apache-2.0.","required_change":"Add pgvector license from its LICENSE file before AI-4; keep the 'not a training-data rights audit' caveat on every HF row."},{"id":"F-09","severity":"low","section":"Evaluation and release rules","claim":"'Freeze thresholds before testing' but no threshold is proposed even where one is not a benchmark question.","why":"Exact-lookup wrong-identity and unauthorized-row tolerance are policy, not measurement; leaving them open invites later negotiation.","evidence":"Draft: 'none of these measurements has been run'.","required_change":"Propose now: AI-1 wrong-identity/unauthorized-location/cost-leak = 0 tolerated; AI-3 constraint violation and fabricated evidence = 0. Latency/recall thresholds stay open pending runs."}],"architectural_position":"Agree with Postgres-as-authority, server-bound ScopeContext, typed allowlisted tools, explicit-preference/content ranking first, hybrid RAG optional, learned models last. Steelman of a memory framework (Mem0/Letta): it delivers extraction, consolidation and per-user retrieval in days and Mem0 ships delete_all scoping (R-05/R-06). It loses because Terroir's memory obligations — business vs personal ownership, purpose boundaries, revision-checked commits, epoch tombstones, no operational facts in memory — are not modelled there; adopting it makes a second authority whose user_id filter is not authorization. Steelman of a full recommender service (Gorse): online/offline split, item-to-item and dashboards out of the box (rec-007/008). It loses because there is zero personal signal data today (verified absent), cold-start would be served by explicit preferences anyway, and it needs its own store/cache, exposure format and permission model outside RLS. The general agent design is correctly rejected. Non-negotiable additions: LLM is a phrasing layer for AI-1..3; authoritative facts are rendered from tool result fields, never from model text; every persisted decision record references evidence IDs.","technology_decisions":{"orchestration":"Vercel AI SDK as a thin adapter with tool/turn/time budget; acceptable, Apache-2.0 by LICENSE text (GitHub detector says Other). Provider unselected; not a blocker for AI-1..3 under F-03.","retrieval":"tsvector + pgvector hybrid with RLS-scoped exact scan first (R-01..R-04). Approximate index only after recall/isolation measurement; iterative scan is not authorization.","embeddings":"Qwen3-Embedding-0.6B/Reranker-0.6B primary trial at 512 dims as hypothesis; multilingual-e5-small and bge-small+MiniLM as controls; EmbeddingGemma only after Gemma-terms review (HF-05); BGE-M3 deferred. No trial before a corpus exists (F-06).","recommendation":"Content/explicit ranking in TypeScript inside the app. RecTools offline harness (Python, out of deployable) for AI-6; LightFM/implicit as challengers; Gorse/TFRS deferred. Serving path = batch to Postgres by default (F-07).","memory":"Borrow lifecycle/provenance concepts from Mem0/Graphiti/Letta; adopt no framework; no graph DB.","ratings":"Like/dislike + optional stars + tags with scale_version; house 50–100 scores stay separate; house notes never count as personal evidence unless explicitly copied."},"slice_order":["0. Freeze F-contract planning notes (TasteProfile, PersonalSignal, ExposureRecord, RecommendationDecision) in the canonical doc's style; freeze the F-02 experience grain and F-09 zero-tolerance rules. No S work.","1. First admitted slice = whichever unblocks first: AI-2 (needs E-001/E-036/E-038, LLM-free) or AI-1 (needs E-012/E-013 placement projection). Do not hard-sequence AI-1 first.","2. AI-1 and AI-2 complete; common safety matrix (F-05) passes on both.","3. AI-3 explainable suggestions over AI-2 signals with cold-start path; LLM optional.","4. AI-5 cellar mode folds into AI-1/AI-3; menu/buy modes wait on E-018/E-047/E-052–E-054.","5. AI-4 after corpus/rights decision (F-06); embedding trial then.","6. AI-6 only after AI-3 exposure history with eligibility snapshots and a serving-path decision."],"unresolved_unknowns":["Production LLM provider, data-retention contract, per-request bounds and spend ceiling — unselected, uncosted.","Licensed knowledge corpus for AI-4 — none identified.","Experience grain and time window for 'last nine' — undefined.","Household/cross-context/gift grant semantics — F-scale, unscoped.","Legacy restaurant_id → business/venue mapping — not promised by the rebuild.","Approximate-index recall under RLS filtering, peak memory of 0.6B models on Railway, p50/p95 latency — no runs.","Whether a deletion epoch is sufficient for provider-side retention — depends on the unsigned provider contract.","Transcript/model-trace retention policy — not selected."],"agreement_conditions":["Findings F-01, F-02, F-03, F-04, F-05, F-06 addressed in the revised draft; F-07..F-09 recorded as noted.","Draft states explicitly that no AI slice enters S and that S14 manual-receipt evaluation is unchanged.","All thresholds, weights and dimension choices remain labeled proposals until runs exist.","This review does not approve Gate 1 and is not a model-identity claim; the host receipt establishes identity.","Agreement is conditional on the revised text, not on the other reviewer's disposition."]}