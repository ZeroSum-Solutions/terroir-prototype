# Independent verification of Fable 5.1 findings

Scope S and Gate 1 remain **NOT APPROVED**. This was a read-only source/plan review; no application tests, secrets, live systems, Git state, CI, or settings were touched.

Plan/CSV citations resolve from `/Users/zero/projects/terroir/docs/plans/2026-09-05-terroir-rebuild`; repository-source citations resolve from `/Users/zero/projects/terroir`; `fable-audit.md` resolves from this file's directory.

## Finding ledger

| ID | Verdict | Verification and bounded correction |
|---|---|---|
| F1 | **SUPPORTED** | The nine planned PRs are horizontal through contracts, schema, access, backend, then UI; the first complete user-facing receipt/cellar path cannot exist before PR6 (`04-ownership.md:22-32`; `03-delivery.md:52-57`). Use a vertical read spine and write spine, with migrations immediately before their first consumer. |
| F2 | **SUPPORTED finding; proposed fix UNSUPPORTED** | Scope does not say whether C changes the 99 legacy auth consumers, while C owns existing auth/API/UI paths (`01-evidence.md:9`; `04-ownership.md:7`). RLS cannot distinguish a legacy route from a candidate route when both use the same cookie-backed publishable client (`src/lib/supabase/server.ts:14-38`; `src/lib/api/auth.ts:21-31`) and `service_role` bypasses RLS (`AGENTS.md:42-45`; `supabase/migrations/0100_wine_identity_merge.sql:36-42`). Candidate tables can withhold authenticated DML—new tables receive service-role defaults only (`supabase/migrations/0074_public_api_grants.sql:23-27`; `supabase/migrations/0143_invoice_scan_deletion.sql:133-146`)—but new authenticated routes then need narrow, actor/scope-validating RPC capabilities. State an explicit candidate route prefix, leave legacy auth/routes untouched, enumerate every authenticated RPC and service-role capability, and test that legacy endpoints leave candidate tables unchanged. Do not claim route-distinguishing RLS. |
| F3 | **SUPPORTED** | S has no legacy backfill (`02-architecture-data.md:52`), yet a production recovery attempt gates hosted S on Day 3 (`02-architecture-data.md:43-46`; `03-delivery.md:69`). Separate legacy migration restore (F/destructive cutover prerequisite) from candidate-project backup/restore (S prerequisite before real pilot admission; `02-architecture-data.md:27,39,70`). |
| F4 | **SUPPORTED** | E-007 assigns lookup to U (`scope.csv:E-007`), but U owns no APIs/shared context and K owns deferred search (`04-ownership.md:11,13`). Existing `/api/search` is restaurant-scoped (`src/app/api/search/route.ts:248-255`; cellar query at `:76`) and cannot simply become the candidate business/venue boundary. Assign a read-only receiving lookup route/domain to R, consume it from U, and leave legacy search untouched. |
| F5 | **SUPPORTED; estimate unverified** | Gate 1 requires owner settings, spend, venue, staffing, and workload decisions (`START-HERE.md:47-53`), while 56–84/88 are contributor hours and approval lead time is unbounded (`03-delivery.md:15,28,38`). Add human-hours and required owner windows per landing; do not present Fable's 4–8h guess as measured. A Sept 5 T0 makes Days 1–11 span four weekend days, so weekend availability must be explicit. |
| F6 | **PARTIAL** | R/U each receive 4–6h despite real-DB crash/replay and multi-width denied-state coverage (`03-delivery.md:22-23`; `05-ci-tests.md:50-58`). Underestimate risk is credible; 8–12h is judgment, not verified. Re-estimate after characterization and pre-authorize only optional descopes; isolation, idempotency, one mobile plus one desktop path, and evaluation labeling remain mandatory. |
| F7 | **PARTIAL; remedy overreaches** | The 10k/5k/50k, 200-sample workload is provisional (`05-ci-tests.md:58`) and “identical” schemas are unavailable. But E-066 is MUST-SHIP S7 (`scope.csv:E-066`), so performance cannot be marked N/A wholesale. Retain small pilot-shaped receipt, cellar-read, and lookup thresholds plus error, replay, and isolation counts; defer large-load/device-profile claims until justified. |
| F8 | **PARTIAL** | Lease/CAS/heartbeat machinery is heavy (`07-protocol.md:15-21`), but S still plans WIP=2 and multiple author/reviewer sessions (`03-delivery.md:36`; `06-capacity.md:64`), so “no concurrency” is false. Keep one authoritative state file, exclusive writer, exact read/write path claims, and landing receipts; omit timed leases/heartbeats only while exactly one mutating session exists. |
| F9 | **PARTIAL** | Queue, aggregator, merge-group, sharding, and WIP=3 work exceeds Q's S budget (`05-ci-tests.md:20,27-35`). Existing CI already runs on every PR (`.github/workflows/ci.yml:41-50`); full E2E supports manual dispatch (`.github/workflows/e2e-full.yml:24-30`). For S, protect the candidate branch with the existing required check, require freshness, manually run selected/full evidence at exact SHA, and defer queue/sharding. The 390px defect may be deferred only if outside selected S; never waive a selected or required gate. |
| F10 | **SUPPORTED, but incomplete** | Ten rows map DEFERRED outcomes to S: `tests.csv:11,14-15,18,20-22,24,51,53` (E-011, E-016, E-017, E-018, E-019, E-067). Fable omitted `EV-04/E-011` at row 18. Add `s_applicability` or blank S slice requirements, then cross-validate every tests row against `scope.csv:day14_S`; the 57-row structural check missed this (`09-review.md:21`). |
| F11 | **PARTIAL** | E-002 and S1 do require the group/business/venue hierarchy (`scope.csv:E-002`; `03-delivery.md:7`), so “no S user” is too broad. Keep a minimal group entity and business relationship; defer group-wide grant semantics. S authorizes and tests business/venue grants and separate cost capability only. |
| F12 | **UNSUPPORTED** | The 3-supported/10-partial/2-unsupported triage does not show that Gemini Flash caused weak review (`09-review.md:11`), and no stronger Gemini tier is verified (`06-capacity.md:58`). Keep vendor-diverse secondary review and record the backend-ID limitation. Route high-risk SQL/RLS authoring to Opus 5 high and independent correctness verification to Sol xhigh; use Fable low only for the bounded qualification unless evidence changes the lane. |
| F13 | **PARTIAL** | WIP=3/sharding/queue and AWS B/C are future capacity options (`05-ci-tests.md:31,35`; `06-capacity.md:21-25`), so label them F/optional. The 30-day legacy compatibility window is F, but candidate backup/retention still needs an S Gate 1/Gate 2 decision (`02-architecture-data.md:35,39`). |

## Schedule and arithmetic check

Fable's 46–76 author hours and 12–20 review hours add correctly for the eight listed rows. They omit the separate protocol-activation PR required at `fable-audit.md:79`; either fold that work into V0 explicitly or count a ninth landing, its author time, and another 1.5–2.5h review.

The Day-3 V1 checkpoint is possible only at low estimates: V0+M1+V1 consume 14 author hours plus at least 4.5 review hours, before protocol work and Fable's unverified 2–4 owner hours. Dependencies are serialized and T0 is a Saturday. Treat V1 by Day 3 as a stretch checkpoint; M1 landed plus V1 in review is the defensible base checkpoint.

V2a is still a backend slice: R owns it and its exit is the “EV-07 backend half,” while the claimed outcome includes enter/review/confirm UI (`fable-audit.md:41`). Fold the minimal receiving UI into V2a, or rename it a write-backend spine and make the next R+U integration landing the first real user write. This is required before calling the revision vertically sliced.
