```json
{
  "verdict": "AGREE",
  "findings": [],
  "agreement_scope": "Re-read the complete revised text against the supplied Cerebras evidence and the prior-plan constraints (Postgres authority, live SQL stock, typed tools, content-based first, no pilot-scope/approval/Day-14 change, no private-data export). All four Fable findings are closed: cuvée-level evidence now enters only as a labeled generic layer when the source claim is genuinely cuvée-general, unknown-year tastings are not promoted to generic, non-vintage is a distinct state, and coverage/explanations must distinguish layers (F1); value origin, source span, normalization/schema revision, extractor model/prompt revision, reviewer-sample validation gate, and revision/invalidation on extractor change are specified without attributing inferred intensity to authors (F2); point-in-time doc conclusion, live authenticated capability check, doc-page model-name inconsistency treated as non-evidence, and credit mechanics marked unverified (F3); per-source rights manifest separates ingest, extract/embed/store, shared-index vs overlay, retention and display permissions, resolved before the embedding trial (F4). No internal contradictions found: 'no inference spending' is consistent with an unauthenticated 403 probe; public/synthetic trial data classes are consistent with the no-private-export boundary; the constrained-set exact-scan fallback with measured eligible-size/p95 gate is consistent with 'never relax a hard constraint silently' and no unsupported capacity claim; the 80% feasibility floor is explicitly a proposal for ratification with a frozen, non-prunable query set, and ordering (ratify coverage rule → census → defer estimation if unmet) is coherent; owners and reviewers are labeled as proposed. Two non-blocking clarifications, not contradictions: (a) the floor's 'evidence-supported comparable wine' should be read as at least one candidate meeting the ratified minimum coverage rule under mandatory constraints, so the census can run before the metric baseline is frozen; (b) 'may be considered under the current research authorization' for public/synthetic trials should not be read as spend authorization — any actual Cerebras trial call remains a separate billing/provider approval decision, which the text's own 'no inference spending' header already implies. Metric, thresholds, clustering, embedding model and the floor value itself remain correctly deferred to gated trials and ratification."
}
```