# Preparation activation checklist

These artifacts are drafts inside the planning directory. They do not configure a running agent, grant permission or start the14-day clock.

1. Record Gate1 approval scope, timestamp/timezone/deadline, retention, budget and accepted capacity assumptions.
2. Recheck canonical repository/remote/main SHA, existing dirty work, related ownership, checks and candidate routing. Confirm no automatic main deployment from candidate merges.
3. Approve exact proposed protocol/adapter diff. Preserve existing production-env, local-target, generated-types, migration-numbering, required-check and conventional-commit rules; do not copy stale factual status paragraphs as policy.
4. Install only the approved canonical project protocol and thin adapters on the protected candidate integration path. Validate both harnesses load the same version/hash and obey permission boundaries. No global settings or hooks are changed by this package.
5. Admit one complete receipt slice and its minimal predecessors. Issue an exclusive claim with exact files; no process/branch takeover from timeouts alone.
6. Qualify routing on representative synthetic cases and first actual slices. Never rank models by LANE_OK latency or vendor marketing alone. Persist route decisions and all failures.
7. Use independent reviews and objective evidence to grant LAND-GO; owner Gate2 still controls real pilot/production admission.

No dependencies are installed, branches/worktrees created, infrastructure provisioned, migrations applied, CI triggered, credentials read, root adapters activated or code deployed during this preparation turn.
