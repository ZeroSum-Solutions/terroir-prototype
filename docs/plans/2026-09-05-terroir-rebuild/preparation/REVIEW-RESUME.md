# Optional completion of the final Fable reread

Fable's successful audit and conditional endorsement are preserved. Its final reread failed with exit1 and a Claude session-limit message reporting a7pm reset; verify live availability before retrying. No automatic retry, reset credit or paid fallback is scheduled.

Original Claude session: afd0f4d2-85a7-4f94-a836-d36105e25314. Exact model: claude-fable-5-1. Use the existing Claude Code Max OAuth lane at medium effort, tools disabled and strict empty MCP, resuming that original reviewer session. Provide evidence/fable/fable-final-reread-request.txt as stdin. The immutable request hash is in evidence/fable/fable-closure-receipt.json. Do not send app data, credentials or a new author defense.

A fresh agent must first recheck repo/main SHA, planning-only changes and exact packet hash. Capture exit code, reported model, session and full response. A status string of success alone is insufficient: the failed call returned that string alongside exit1 and a quota message. Only a substantive successful finder response counts. The non-author steward records any final acknowledgment; the document author must not close its own findings. No implementation or Gate1 approval is inferred from a successful reread.
