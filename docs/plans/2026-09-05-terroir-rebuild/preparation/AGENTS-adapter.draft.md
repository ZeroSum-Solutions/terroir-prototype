# Codex adapter draft: inactive

Proposed root adapter after approval: load docs/PROJECT-PROTOCOL.md version TERROIR-REBUILD-v2 before editing. If missing/mismatched, stop admission. Preserve the existing root rules until a reviewed replacement carries every applicable rule forward.

Production .env.local is forbidden for tests; use the guarded local start and intended DB/project identity. Codex uses the canonical claim, review, branch and gate rules through its native tools. The protocol version/hash is populated from the approved canonical file during V0, not invented here.
