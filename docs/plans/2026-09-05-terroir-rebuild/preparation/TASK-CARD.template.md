# Vertical slice task card

Status: TEMPLATE, not an admitted task.

- Slice ID / requirement IDs:
- Visible user outcome and explicit exclusions:
- Predecessor evidence / frozen contract version:
- Single accountable owner, author agent/model/effort/harness:
- Branch, worktree, owning machine, starting SHA:
- Exclusive exact files / proposed new paths:
- Schema change needed? D-owned predecessor PR or exclusive temporal handoff:
- Characterization and target acceptance tests BEFORE implementation:
- Failure, permission, replay and recovery cases:
- Focused builder command and intended synthetic DB identity:
- Author-work estimate; review/fix estimate; elapsed wait estimate (separate units):
- Independent reviewer pair and non-author findings steward/lander:
- Evidence manifest: exact head/base SHA, fixture hash, results including skips:
- Replan/descope trigger and safe stopping state:
- Lease / handoff record:

A UI without its real persistence and negative cases does not complete the slice.
Changing this card's requirement or acceptance criteria requires a recorded scope decision.
