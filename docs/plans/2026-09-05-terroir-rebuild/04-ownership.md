# Ownership, vertical boundaries and dependency graph

All branches/worktrees below are proposals. None is created by this package. After Gate 1 use an approved new root such as /Users/zero/projects/terroir-rebuild-slices, never the excluded owned directories. One machine owns each branch at a time; the measured Mini is the only committed builder. MacBook handoff requires verified access/capacity and a recorded release.

| Slice / role | Proposed branch; harness | Exclusive ownership specification |
|---|---|---|
| V0 / Q | rebuild/rails; Codex | Canonical protocol and thin root adapters, required candidate CI/protection specification, shared fixture and safe test scripts. Exact 390px exception below. No generic business logic. |
| M1, M2 / D | rebuild/scope-schema then rebuild/receipt-schema; Claude | All numbered SQL/down migrations, schema snapshots/manifests and generated DB types; new candidate stock transaction adapter/domain. Sole schema authority. |
| V1 / C | rebuild/scoped-cellar; Codex | New candidate scope contract/resolver, invited bootstrap/sign-in, candidate host route policy and initial scoped cellar read/UI. Existing legacy auth consumers remain unchanged. |
| V2 / R | rebuild/receiving; Codex by default | Candidate-only product lookup domain/API, manual receipt application/API and receiving/cellar UI. C transfers the exact candidate cellar paths before V2. Full-stack ownership prevents a second UI queue. No SQL/generated types. |
| V3 / O | rebuild/evaluation-operations; Claude | Dedicated candidate revoke/export/pause operations, scoped CSV, recovery runbook and operator actions. D's transaction contract remains unchanged unless a newly estimated schema handoff is admitted. |
| V4 / Q | rebuild/candidate-verification; Codex | Integration tests/evidence/deployment and recovery documentation. Behavior changes need an explicit file transfer; no miscellaneous catch-all fixes. |

Before every admission replace descriptions with exact tracked files and proposed new directory names. Proposed candidate route roots are /pilot and /api/pilot; resolve actual Next route group and deployment policy in V0/V1 before editing. Shared files never have two current owners. Authors own adjacent tests inside their paths; Q owns shared harness files. Q/C and C/R transfers are explicit and exclusive. One active application builder globally; independent packet reviewers can run concurrently.

Q alone holds the V0 390px repair exception for src/app/(app)/insights/date-range-selector.tsx, src/app/(app)/insights/page.tsx, adjacent date-range-selector.test.tsx and e2e/one-row-rule.test.ts. Charge repair inside V0. If the coherent repair needs another PR or exceeds capacity, re-estimate; do not hide it in stabilization. Preserve the test. A legacy unrelated failure must remain a named failure with scope evidence, never a claimed green full suite.

R owns E-007's **candidate receiving lookup backend and UI**, over an approved catalog projection, not the legacy broad search endpoint. D owns identity/package schema. No public lookup/private inventory join or label scanner is implied. E-006 ambiguity/merge tooling is deferred; S rejects unsupported/ambiguous choices without guessing. Schema D supplies only immediate consumer contracts; no speculative job, Toast or public implementation is scheduled.

Full-release ownership, admitted later: K public identity/knowledge/search/map; T Toast ingress/mapping/jobs; V counts/transfers/pours; P reporting/menus/pricing; U richer cellar placement/context UX; O wider lifecycle/offline. These remain unassigned future tracks, with exact file transfers and estimates required before work. The earlier broad C ownership over 99 legacy auth consumers is withdrawn for S.

See dependency-graph.mmd and dependency-edges.json. The S critical path is G1 → V0 → M1 → V1 → M2 → V2 → V3 → V4 → G2. This intentionally serializes schema and mutation work; two ready PR slots are a ceiling, not a promise of parallel speed. Future dependencies break feature cycles through scoped stock, consumption/count and publication contracts; a DAG edge does not authorize implementation of deferred features.
